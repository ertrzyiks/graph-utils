{
  "name": "@ertrzyiks/graph-utils-root",
  "version": "0.0.0",
  "description": "",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "lerna": "^3.22.1"
  },
  "scripts": {
    "start": "lerna run start --scope website --stream",
    "build": "lerna run build --scope website --stream",
    "codegen": "lerna run codegen --scope website --stream"
  }
}
